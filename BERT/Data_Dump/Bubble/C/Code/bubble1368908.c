#include <stdio.h>

#define DEF_ELM_MAX 100

	int R[DEF_ELM_MAX];

void swap(int *x, int *y)
{
	int tmp;
	tmp = *x;
	*x = *y;
	*y = tmp;
	return;
}

int bubbleSort(int *A, int N) // N ??????????´?????????? 0-????????????????????? A
{
	int flag = 1; // ????????£??\????´?????????¨??????
	int i;
	int j;
	int cnt = 0;

	i = 0; // ??????????????¨?????????????????????????????????
	while (flag) {
		flag = 0;
		for ( j = N-1 ; j > i ; j-- ) {
			if (A[j] < A[j-1]) {
				swap(&A[j], &A[j-1]);
				cnt++;
				flag = 1;
			}
		}
		i++;
	}
	return cnt;
}

int main(void)
{
	int N;
	int i;

	int ans;

	scanf("%d", &N);
	for ( i = 0 ; i < N ; i++ ) {
		scanf("%d", &R[i]);
	}

	ans = bubbleSort(R, N);

	for ( i = 0 ; i < N ; i++ ) {
		if (i > 0) {
			printf(" ");
		}
		printf("%d", R[i]);
	}
	printf("\n%d\n", ans);
	return 0;
}
