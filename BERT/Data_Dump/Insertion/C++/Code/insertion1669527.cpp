#include <iostream>
#include <algorithm>

using namespace std;

     //??????????¶????
void out (int a[],int n){
    for (int i = 0; i < n-1 ; ++i) {
        cout << a[i] << " ";
    }
        cout << a[n-1] << endl;
}

int main() {
    int a[100];
    int n;

    //??\?????¨
    cin >> n;
    for (int i = 0; i < n ; ++i) {
        cin >> a[i];
    }
    //???????????¶?????????
    out (a,n);

    //?????\?????????
    for (int i = 1; i < n ; ++i) {
        int j = i-1;  //????¨????????????????
        int v = a[i]; //?¬???????????????????????¨???¶

        // ????¨????????????????????????? ?????? ????¨??????????????????????????????????????????§???????????????????????????
        while( j >= 0 and a[j]>v) {
            a[j + 1] = a[j];
            --j;
        }
        a[j + 1] = v;
        out (a,n);
        }
    return 0;
}
