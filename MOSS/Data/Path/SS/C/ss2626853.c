#include<iostream>
using namespace std;
int V, E, R, S[500010], T[500010], D[500010]; // ????????§?????????????????\???
int C[100010]; // ??????????????§??????????????¢?????????
// ???????????¨????????°?????¨???????????????????????§?¶?????¨????
const int Inf = 10000*100000+100;

int main() {
  cin >> V >> E >> R;
  for (int i=0; i<E; ++i)
    cin >> S[i] >> T[i] >> D[i]; // ???????????\??? 
  for(int i=0;i<V;i++)
    C[i]=Inf;
  C[R]=0;// C????????????: C[R]???0???,??????Inf???
  for (int t=0; t<V; ++t)
    {                           // V?????°?????????
      bool update = false;
      for (int i=0; i<E; ++i)
      {
	int s = S[i], t = T[i], d = D[i]; // i???????????????s,t,d????????????
	if (C[s] < Inf && C[t]>d+C[s])
	  { // ???s,t??§????????§????????????
	    C[t] =d+C[s]; // C[t]?????´??°;
	    update = true; // ??´??°???????????£????????¨????¨????
	  }
      }
      if (!update) break; // ????????????????????´??°??????????????°???????????£?????????
    }
  for(int i=0;i<V;i++)
    {
      if(C[i]==Inf)
	cout<<"INF"<<endl;
      else
	cout<<C[i]<<endl;
    }
}
