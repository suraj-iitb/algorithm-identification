#include <iostream>
#include <cstdio>
#include <vector>
#include <queue>
#include <algorithm>
#include <functional>
#include <climits>
#define FOR(i,a,b) for(int i=(a);i<int(b);i++)
using namespace std;

const int MAX_V = 100000; // ???????????°????????§???
const int MAX_E = 500000; // ????????°????????§???
const int INF = INT_MAX;

struct E{
  int to, cost;
  E(int t, int c) : to(t), cost(c){};
};

typedef pair<int,int> pii;

int v; // ????????°
vector<E> g[MAX_V]; // ??£??\?????????
int d[MAX_V]; // ??????s????????????????????¢

void dijkstra(int s)
{
  fill(d,d+v,INF);
  d[s] = 0;
  priority_queue<pii,vector<pii>,greater<pii> > q;
  q.push(pii(0,s));

  // ??????s????????????????????????????????¢????¨????
  while(!q.empty()){
    /* ???????¢???????????????????????????????????????¢???????°???????????????¢??? */
    pii p = q.top();
    q.pop();
    int from = p.second;
    if(d[from] < p.first) continue; // ???????????¢??§???????????°??????
    
    FOR(i,0,g[from].size()){
      int to = g[from][i].to;
      int cost  = g[from][i].cost;

      if(d[to] > d[from] + cost){ // ???????????¢??¨??????????????´???
        d[to] = d[from] + cost;
        q.push(pii(d[to],to)); // ???????????¢???????£?????????????????????\
      }
    }
  }
}

int main()
{
  int gv, ge, gr; // gv???????????°???ge???????????°???gr????§????
  cin >> gv >> ge >> gr;
  v = gv;
  FOR(i,0,ge){
    int gs, gt, gd;
    cin >> gs >> gt >> gd; // ge??????????§???????gt??????????????????gd???????????????
    g[gs].push_back(E(gt,gd));
  }

  dijkstra(gr);

  FOR(i,0,v){
    if(d[i] != INF)
      cout << d[i] << endl;
    else // ??????i?????????????????´???
      cout << "INF" << endl;
  }
}
