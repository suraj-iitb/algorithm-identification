#include  <stdio.h>
#define N 100

int main(){
	int i,j,n,temp,A[N],cnt=0;
	scanf("%d",&n);
	
	for(i=0;i<n;i++)
		scanf("%d",&A[i]);
		
		//?????????????????????
		for (i = 0; i < n - 1; i++) {
        	for (j = n - 1; j > 0; j--) {
            	if (A[j - 1] > A[j]) {  /* ??????????´????????????§????????£?????? */
                	temp = A[j];        /* ???????????? */
                	A[j] = A[j - 1];
                	A[j - 1]= temp;
                	cnt++;
            	}
        	}
    }
	 for (i = 0; i < n-1 ; i++)
        	printf("%d ", A[i]);
        	printf("%d\n",A[i]);
        	printf("%d\n",cnt);
	return 0;
}
