#include <stdio.h>

#define NUM_ITEMS 100

void selectionSort(int *, int array_size);
void swap(int *,int*);

int main()
{
  int i,j,num;
  int a[100];

  scanf("%d",&num);

  // fill array with random integers
  for (i=0;i<num;i++) {
    scanf("%d",&a[i]);
  }

  // perform selection sort on array
  selectionSort(a, num);

  return 0;
}

void selectionSort(int *a, int array_size)
{
  int i; // ??????????????????????????????????????????
  int j,k; // ?????????????´????????????????????????????
  int min; // ????°?????????????????´??????????????????????
  int temp; // ????????¨??????????????°
  int step=0;

  for (i = 0; i < array_size-1; i++) {
    min = i; // ??????????????????????°???????????´???¨??????

    for (j = i+1; j < array_size; j++) { // ??????????????????

      if (a[j] < a[min]) {
        min = j; // ????°?????????????????´??????´??°
      }
    }
//  for(k=0;k<array_size;k++){
//    printf("%d ",a[k]);

//  }
//    printf("\n");
    // ????°?????????????????´?????????????????´???¨??????
    swap(&a[i],&a[min]);
if(i!=min)    step++;


  }

  for (i = 0; i < array_size-1; i++) {
    printf("%i ", a[i]);
  }
  printf("%d\n",a[i]);
  printf("%d\n",step);

}

void swap(int *a,int *b){
  int tmp;
  tmp = *a;
  *a = *b;
  *b = tmp;
  }
