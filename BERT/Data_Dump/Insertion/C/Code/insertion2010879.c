#include<stdio.h>

int main(void) {

	int a[100], n, i, j, k, key;
	/*printf("n???????????\????????????????????????\n");*/
	scanf("%d", &n);
	/*printf("%d??????????????\????????????????????????\n",n);*/
	for (i = 0; i < n; i++) 
	scanf("%d", &a[i]);

	for (k = 0; k < n; k++) {

		key = a[k];

		j = k-1;

		while (j >= 0 && a[j]>key) {
			a[j + 1] = a[j];
			j--;
		}
			a[j + 1]=key;
		for (i = 0; i < n; i++)
			printf("%d%c", a[i],i==n-1?'\n':' ');
	}
	return 0;
}
